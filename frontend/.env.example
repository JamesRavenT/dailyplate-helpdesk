# Sentry (leave blank to disable error reporting)
VITE_SENTRY_DSN=

# Required only when running `vite build` with source map uploads
SENTRY_AUTH_TOKEN=
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=your-project-slug
